This command has to be used when deploying several resources into a specific (new) ResourceGroup, using an ARM-template.

Replace the placeholder-values in the variables by your own SubscriptionName/ResourceGroupName/Template-path before executing the script.